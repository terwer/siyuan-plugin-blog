import { zhiBlogMiddlewareEsbuild } from "./zhi-blog-middleware-esbuild"

describe("zhiBlogMiddlewareEsbuild", () => {
    it("should work", () => {
        expect(zhiBlogMiddlewareEsbuild()).toEqual("zhi-blog-middleware-esbuild")
    })
})
